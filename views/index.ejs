<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Render Country By name</title>
    <link rel="stylesheet" href="styles/style.css" />
  </head>
  <body>
    <div class="container">
      <!-- display this box only if country is not rendered  -->
      <% if(locals.name){ %> <% null %> <% }else { %>

      <div class="box">
        <!-- if error message is sent then show this error  -->
        <% if(locals.err) { %>
        <span class="error">Country Name Not found. Try again!</span>
        <% } %>
        <h1>Render Country By Name</h1>
        <form action="/submit" method="post">
          <input
            type="text"
            name="countryName"
            placeholder="Enter Country Name"
            class="name"
          />
          <input type="submit" value="Render" class="submit" />
        </form>
      </div>

      <!-- if country is obtained from API render it  -->

      <% } %> <% if(locals.name){ %>
      <div class="display">
        <img src=" <%= locals.image %> " alt="" />
        <h1>Name: <%= locals.name %></h1>
        <h3>Population: <%= locals.population %></h3>
        <h3>Border: <%= locals.border %></h3>
        <h3>Continent: <%= locals.continent %></h3>
      </div>
      <% } %>
    </div>
  </body>
</html>
